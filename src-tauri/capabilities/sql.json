{
  "$schema": "https://tauri.app/schema/capability.json",
  "identifier": "sql-cap",
  "description": "Permisos para usar plugin-sql",
  "windows": ["main"],
  "permissions": [
    "sql:default",
    "sql:allow-execute"
  ]
}
